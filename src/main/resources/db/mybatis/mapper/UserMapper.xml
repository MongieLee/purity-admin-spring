<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.example.demo.User">
    <select id="login" parameterType="User" resultType="User">
        select *
        from user
        where id = #{username}
          and encryptedPassword = #{encryptedPassword}
    </select>
    <insert id="register" parameterType="User">
        insert into user
            (username, encrypted_password, status, created_at, updated_at)
        values (#{username}, #{encryptedPassword}, 1, now(), now())
    </insert>
    <select id="getUserById" resultType="User">
        select *
        from user
        where id = #{userId}
    </select>
    <select id="getUserByUsername" resultType="User">
        select *
        from user
        where username = #{username}
    </select>
    <select id="getPage" resultType="User">
        select *
        from user
        <if test="username != null">
            where username like concat('%',#{username},'%')
        </if>
    </select>
    <update id="updateUser" parameterType="User">
        UPDATE user
        SET avatar     = #{avatar},
            updated_at = now()
        where id = #{id}
    </update>
    <delete id="deleteUser">
        delete
        from user
        where id = #{id}
    </delete>
    <update id="changeStatus">
        update user
        set status = #{status}
        where id = #{userId};
    </update>
    <select id="findRolesByUserId" resultType="long">
        select role_id
        from user_role_rel
        where user_id = #{userId};
    </select>
</mapper>
